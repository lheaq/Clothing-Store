Clothing Store is a Java Swing desktop application that simulates a basic clothing store ordering system. It features a three-page GUI flow — a home screen, an order page with cascading dropdowns for filtering products by category and subcategory, and a receipt page that displays all selected items along with the computed total. Product data is managed through a separate StoreProducts class, and orders are tracked using a LinkedList throughout the session.
